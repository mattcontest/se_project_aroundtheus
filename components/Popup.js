export default class Popup {
  constructor({ popupSelector }) {
    this.popupElement = documunet.querySelector(popupSelector);
  }

  open() {
    //opens popup
  }

  close() {
    //closes popup
  }

  _handleEscClose() {
    //listens for escape button
  }

  setEventListeners() {
    //setse vent listeners
  }
}
